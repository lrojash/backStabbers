import React from 'React'
import TextInput from './TextInput'

export default ({ props }) => {
    return (
        <section className="comments-post">
            {/* {will pass down the text input and place here } */}
            {/* for now temp text */}
            commet comment comment comment
        </section>
    )
}